# v1.7.0 — QR 扫码登录 + 快速开始

**发布日期**：2026-09-19

## 新功能

### QR 扫码绑定机器人
- 控制台执行 `huhobot qr` 或 WebUI「配置 → 扫码绑定」生成二维码，手机 QQ 扫码即可自动写入 AppID/Secret
- 控制台直接打印二维码字符画（半块字符渲染，紧凑显示），同时生成 `qr-login.svg` 备用
- 启动时若 `bot.app-id`/`bot.secret` 为空，自动触发扫码流程（`bot.auto-qr` 默认开启）
- 二维码过期自动刷新，支持 `huhobot qrcancel` 取消

### WebUI 快速开始页（Llama 版）
- 未配置凭据时，登录后直接进入快速开始引导页
- 两种方式：📱 扫码绑定（生成二维码）/ ✏️ 手动填写（AppID + Secret 表单）
- 绑定成功后自动重载并跳转主界面

## 改进

- QR 编码使用经过验证的 `qrcode` 库核心模块，确保生成的二维码可正常扫描
- 终端二维码使用半块字符（▀▄█）渲染，高度减半，更适合 BDS 控制台显示

## 配置新增

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `bot.auto-qr` | `true` | 启动时若未配置凭据，自动打印二维码引导绑定 |

## 控制台命令

| 命令 | 说明 |
|------|------|
| `huhobot qr` | 启动扫码绑定，控制台打印二维码 |
| `huhobot qrcancel` | 取消进行中的扫码会话 |

## 升级说明

- 从 v1.6.0 直接覆盖升级即可，无需修改配置文件
- 新增 `bot.auto-qr` 配置项，不影响现有配置
- WebUI 快速开始页仅在 `bot.app-id` 为空时显示，已配置的用户不受影响
